/**
 * Selector Editor UI - рендер и логика редактора селекторов (встроено в ConfigSidebar)
 * Этот блок заменяет placeholder renderSelectorEditor полноценной разметкой и обработчиками.
 */

// Вставляется в класс ConfigSidebar: заменить метод renderSelectorEditor() и расширить bindEventListeners()
